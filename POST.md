# 关于 Jev，大家普遍用错的一件事

我用 TypeSafe 的 System One 模型 Jev 跑了一个近五年的股票策略回测，
最后做到 **+107.8%**，同期标普 500 是 +59.5%。

但这篇不是来晒这个数字的。真正值钱的是过程中发现的一件事：
**我前面几个月一直在"调提示词"，以为是在教她怎么交易。
其实她从来不需要被教——我只是一直在用错的方式问她问题。**

而让我看明白这一点的，是一个和股票毫无关系的问题。

---

## 先做个实验：问她"今天星期几"

这个实验你自己就能跑，五分钟，总共花不到 0.0001 美元。
完整代码在 [`examples/what_day_is_it.py`](examples/what_day_is_it.py)，
核心就这么几行：

```python
from typesafe_sdk import TypeSafeClient, Choice, Noul

client = TypeSafeClient(api_key=os.environ["TYPESAFE_API_KEY"])
DAYS = ("Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday")

# 同一个问题，两套选项：一套逼她必须挑一天，一套允许她说"说不出来"
FORCED    = Choice(instructions="What day of the week is it today?",
                   criteria={d: f"Today is {d}." for d in DAYS})
WITH_EXIT = Choice(instructions="What day of the week is it today?",
                   criteria={**{d: f"Today is {d}." for d in DAYS},
                             "cannot_tell": "Nothing in the text says what day "
                                            "it is, and it cannot be worked out."})

client.system_one(state=" ", questions={"weekday": FORCED}, model="jev-1.13.0")
```

跑出来是这样（你的数字会有小幅出入，原因见下面第 6 条）：

**① 不给任何背景，也不给退路**

```
weekday   Monday   置信度 0.14      ← 七选一瞎猜的基线正好是 0.14
          Monday 0.26  Wednesday 0.23  Sunday 0.17
```

她答了"Monday"。那天恰好真是星期一——但**置信度和瞎猜一样**，
概率几乎平摊在七天上。她不是知道，她是**被迫必须从七个里挑一个**。

**② 同样不给背景，但在选项里加一个"说不出来"**

```
weekday   cannot_tell   置信度 1.00
          cannot_tell 1.00   Thursday 0.00   Saturday 0.00
```

**她一直都知道自己不知道。她只是没有地方可以说出来。**

**③ 把答案放进背景里**

```
state: "It is Thursday afternoon."
weekday   Thursday   置信度 1.00
```

**④ 背景里写什么，她就信什么**

```
state: "Today is Monday."   →   is_monday  0.96
state: "Today is Friday."   →   is_monday  0.00
```

同一个模型、同一个问题。**唯一的变量是那句背景。**

**⑤ 背景自相矛盾时**

```
state: "Today is Monday. Today is Friday."
weekday   Monday   置信度 0.39
          Monday 0.48   Friday 0.41   Wednesday 0.09
```

她没有硬选一边装作确定，而是把概率分给两个说法，置信度自己掉下来。
**她的诚实全部写在置信度里。**

**⑥ 同一个请求连问六次**

```
Wed/0.13   Mon/0.16   Mon/0.12   Mon/0.15   Mon/0.13   Mon/0.13
```

**她不是确定性的。** 而且注意第一次的赢家是 Wednesday——
当置信度贴近基线时，**连"哪个选项赢"本身都是不稳定的**。
这也正是为什么你不该盯着答案看。

（我自己就栽在这上面：这篇文章早先的版本里引用过一个 0.11 的数字，
后来重跑时拿到 0.22、0.49、0.60。那个问题问得有歧义，
所以答案不稳定。上面留下的都是我反复跑过、稳定复现的部分。）

**结论：Jev 没有时钟、没有日历、没有记忆。
她所知道的一切，都是你在这一次请求里递给她的那段文字。**

---

## 那 Jev 到底是什么？

她不是聊天模型，不生成文字。你给她一段**状态**和若干个**问题**，
她在 70–500 毫秒内返回**带标定概率的类型化答案**。三种基本类型：
`Noul`（某个命题为真的概率）、`Score`（有序刻度上的位置）、
`Choice`（从给定选项里选一个，并返回完整概率分布）。

三个特性决定了上面那个实验的结果，也决定了后面所有的用法：

1. **她无法被微调。** 所有"专业能力"都来自你在请求里写的那段说明。
2. **每次调用完全无状态。** 她不记得上一次，也没有"现在"的概念。
3. **`Choice` 必须选一个。** 你的选项里如果没给"不知道"留位置，
   那份不确定性**一定会落到某个看起来像决定的选项上**。

---

## 所以，到底该怎么用？

下面每一条我都是踩过坑之后才明白的，附上代价。

### 1. 永远留一个"不知道"的出口

这是第一条，也是最便宜的一条。没有出口，弃权会伪装成决定。

我的卖出判断里有三个选项：持有 / 卖出 / 说不清。而"说不清"在代码里
被归到了卖出。结果就是：**三个选项里有两个是卖。**
Jev 被问了 62 次要不要卖，62 次都说卖。

### 2. 看置信度，而且要跟"随机猜"比

答案本身几乎不携带信息量，`Choice` 反正总会给你一个。
**信息全在置信度里，而且必须和均匀分布比。**

七选一的基线是 0.14，五选一是 0.20，三选一是 0.33。
我的形态识别当时是 0.257——听起来像个数，
但对照 0.20 的基线，就知道那基本是在猜。

而且别忘了上面实验⑥：**她不是确定性的。**
置信度接近基线的时候，同一个请求连赢家都会变。
所以"她选了 A"这件事,在低置信度下**根本不构成信息**。

### 3. 千万不要把结论写进状态里

这是我犯过最贵的错。当时问她要不要卖，提示词开头是：

> "机械规则**已经决定卖出**这个持仓了。你要推翻这个决定吗？"

这和在背景里写"今天是星期一"、再问"今天是星期一吗"是**同一个操作**。

同一个模型、同一批持仓，只换措辞：

| 提问方式 | 选择继续持有 | 平均置信度 |
|---|---|---|
| 中性："这个持仓现在在做什么？" | 46.0% | 0.38 |
| 推翻式："要推翻这个卖出决定吗？" | **2.4%** | **0.90** |

**保留率塌了 20 倍，置信度反而从 0.38 涨到 0.90。**
她不是在判断走势，她是在很有把握地同意我写的那句话。

> **置信度高不一定是好事。** 当你把答案藏在题干里，
> 她会非常自信地把它还给你。

### 4. 该给的证据，一定要真的给

我的代码里曾有一行注释：盈利数据做不了，因为拿不到退市公司的历史财报。
这句话对我当时用的数据源成立，**对 SEC EDGAR 完全不成立**——
那上面每条财务数据都带着**它被提交的日期**，
所以"某一天投资者能看到什么"是可以精确还原的，一点都不用猜。

补进去之后：那个筛选条件把 60 天后的收益拉开了 4.8 个百分点
（置换检验 p = 0.0095），而 **Jev 的判断和纯算术的结论吻合度 95%**。

**她一直都读得懂财报。只是之前没人给过她。**

### 5. 判断交给她，算术留给代码

我曾经设了一个"已经涨太多"的选项，结果它**一次都没被选中过**，
96 个测试样本里，12 个明显涨过头的全被判成"正常"。

原因不是措辞不好。原因是**"涨了多少"是个算术问题**——
代码里明明存着基准价，一减就出来了。
我却把一个事实拿去问她的意见，还让它和另一个选项抢概率。

删掉那个选项，改成代码里的一个阈值，问题就没了。

> **能算出来的，别问她。** 她的价值在"这个形态像不像一个休整"，
> 不在"这个数比那个数大多少"。

### 6. 结论要冻结，不要每次重算

我的提示词里有一句"当前价格距离突破基准 X%"。
问题是这个"基准"每次都用最近 65 天的最高点重新算一遍——
而这个数会随着股价一起往上爬。

某个真实持仓在突破后第 10 天，实际还在基准之上 +0.1%，
提示词里却写着 −3.3%。**连正负号都是错的。**

**当时判断了什么，就冻结下来往后传，不要在每一环重新推导一遍。**

### 7. 报错不是决定，弃权也不是

我的代码里，调用失败被当成了"继续持有"。
那意味着**一次 API 故障 = 全仓死拿**，而且程序还会若无其事地
打印出一个最终收益数字。

现在：失败交还给机械规则、单独计数，
整轮失败率超过阈值就直接打印"这一轮不算数"。
连续弃权也有上限——但**有把握的持有不设上限**，
因为拿住一只强势股几个月本来就是策略在正常工作。

### 8. 先用小样本验证，而且把预期先写下来

改提示词是有代价的：提示词一变，指纹就变，全部缓存作废。
我一次一次地改，跑了四轮全量，烧掉 6.8 美元——
其中大约 4.5 美元是纯粹浪费的，因为那四次改动本该合成一次。

现在的做法：**所有改动攒在一起 → 300 条样本验证（约 3 美分）→
措辞冻结后才跑一次全量。** 而且把预期结果**先提交到 git**，
免得事后把失败讲成成功。

这一套做完，验证全部改动只花了 0.05 美元。

---

## 那 +107.8% 到底算不算数？

说点扫兴的：**我不打算把它当成"跑赢了"。**

这个组合只有 5 个仓位、一年约 22 次买入，
**碰巧买到哪几只的影响大到离谱**。所以我做了刀切法：
从股票池里随机删掉 49 只，重算全部信号，
让两个版本**在同一个被删过的池子上各跑一遍**，在每次抽样内部做差。

```
原始池子      Jev − 机械规则 = +$37,512
12 次抽样     均值 +$4,048，标准差 $25,029，Jev 领先 7/12
```

**7 比 5，和抛硬币没区别。** 那个漂亮数字是一次运气不错的抽样。

但同样这个测量，在我做这些修正**之前**是：
**均值 −$23,315，只领先 1/12**——统计上显著地更差。

> 所以真正被证明的是这一句：
> **把她缺的证据补上、把我塞进题干里的结论拿掉，
> 让 Jev 从"稳定地不如机械规则"变成了"分不出高下"。
> 这是真实的进步，但它还不是优势。**

---

## 一句话

Jev 不需要你教她怎么思考。她需要你**别把答案写在题干里**，
**把该给的信息真的给她**，然后**看着置信度，而不是答案**。

这三件事，问一句"今天星期几"就能全部看明白——
代价是五十万分之一美元。
