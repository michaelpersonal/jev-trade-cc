# Jev 只知道你告诉她的事

我用 TypeSafe 的 Jev 跑了一个 O'Neil 动量策略的回测。2022 年 1 月到 2026 年 9 月，10 万美元变成 207,746 美元，同期标普 500 是 159,500 美元。

这个数字不是本文的重点，文末会讲它为什么不能全信。重点是过程中我搞明白的一件事：我花了几个月调提示词，以为问题出在措辞上，真正的问题是我给她的状态和选项。她不需要被教怎么交易，她需要被正确地提问。

一个跟股票无关的问题，把这件事讲得最清楚。

## 问她今天星期几

你可以自己跑。完整代码在 [`examples/what_day_is_it.py`](examples/what_day_is_it.py)，五分钟，成本不到 0.0001 美元。核心是同一个问题配两套选项：

```python
from typesafe_sdk import TypeSafeClient, Choice, Noul

client = TypeSafeClient(api_key=os.environ["TYPESAFE_API_KEY"])
DAYS = ("Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday")

FORCED = Choice(instructions="What day of the week is it today?",
                criteria={d: f"Today is {d}." for d in DAYS})
WITH_EXIT = Choice(instructions="What day of the week is it today?",
                   criteria={**{d: f"Today is {d}." for d in DAYS},
                             "cannot_tell": "Nothing in the text says what day it is, "
                                            "and it cannot be worked out."})

client.system_one(state=" ", questions={"weekday": FORCED}, model="jev-1.13.0")
```

我这边跑出来的结果（你的数字会有小幅差异，原因见第 ⑥ 条）：

```
① 不给背景，只能从七天里选            Monday       置信度 0.14   (七选一随机基线 0.14)
② 不给背景，但多一个"说不出来"        cannot_tell  置信度 1.00
③ 背景："It is Thursday afternoon."   Thursday     置信度 1.00
④ 背景："Today is Monday."  问"今天是周一吗"   0.96
   背景："Today is Friday."  问同一个问题       0.00
⑤ 背景："Today is Monday. Today is Friday."   Monday 0.48 / Friday 0.41
⑥ 请求 ① 连跑六次    Wed/0.13  Mon/0.16  Mon/0.12  Mon/0.15  Mon/0.13  Mon/0.13
```

① 里她答了 Monday，那天恰好真是周一，但置信度和随机猜一样。她并不知道，只是 Choice 规定必须选一个。② 只多了一个选项，她马上以 1.00 表示说不出来。④ 说明背景里写什么她就信什么。⑥ 说明她不是确定性的，置信度贴近基线时，连哪个选项胜出都会变。

原因很简单。Jev 没有时钟、日历和记忆，每次调用都是无状态的，也不能被微调。她知道的全部，就是这一次请求里的 state 和问题本身。她返回的是带概率的类型化答案：`Noul` 给出一个命题为真的概率，`Score` 给出有序刻度上的位置，`Choice` 在你给定的选项中选一个，并附上完整分布。

这决定了该怎么用她。下面四条，每条都来自我在交易系统里犯过的错。

## 1. 给不确定性留一个出口

`Choice` 一定会选一个。选项里如果没有"说不清"，她的不确定就会落在某个看起来像决定的选项上。

我的卖出判断有持有、卖出、说不清三个选项，代码却把"说不清"当成卖出，于是三个选项里有两个是卖。她被问了 62 次，62 次都是卖。

出口要有，出口通向哪里也要写清楚：弃权就是弃权，单独记录，不能被悄悄换成某个动作。调用失败也一样。我的代码曾把 API 报错当成"继续持有"，一次故障就会让所有持仓一直拿着，程序还照常打印收益。

## 2. 读置信度，并和随机基线比较

答案本身信息量很小，信息在置信度里。七选一的随机基线是 0.14，五选一是 0.20，三选一是 0.33。我的形态识别一度是 0.257，看着像个数，对照 0.20 其实接近在猜。

置信度也不是越高越好。下一条就是反例。

## 3. state 里放事实，不放结论

我曾经这样问她要不要卖："机械规则已经决定卖出这个持仓，你要推翻吗？"这和在背景里写"今天是周一"、再问"今天是周一吗"是同一回事。

同一个模型、同一批持仓，只改提问方式：

| 提问方式 | 选择继续持有 | 平均置信度 |
|---|---|---|
| 这个持仓现在在做什么？ | 46.0% | 0.38 |
| 要推翻这个卖出决定吗？ | 2.4% | 0.90 |

保留率降到原来的二十分之一，置信度却从 0.38 升到 0.90。她很有把握地同意了我写进题目里的结论。

事实本身也要冻结。我给她的"距离突破点多少"是每天用最近 65 天的高点重算的，而这个高点会跟着股价上移。一个持仓在突破后第 10 天实际高出基准 0.1%，提示词里写的却是 −3.3%，符号都反了。做判断时用的基准，要原样往后传。

## 4. 缺的证据补上，能算的交给代码

O'Neil 的方法看盈利增长，我一开始没给她，因为以为拿不到退市公司的历史财报。SEC EDGAR 其实有，而且每条财务数据都带提交日期，能精确还原某一天投资者看得到什么。补上以后，她对"盈利增长是否达标"的判断和直接计算的结果 95% 一致。这个条件本身在 353 个买入信号上，把 60 天后的收益拉开了 4.8 个百分点（置换检验 p = 0.0095）。

反过来，能算出来的就不要问她。我设过一个"已经涨太多"的选项，96 个测试样本里一次都没被选中，12 个明显追高的样本全被判为正常。价格离基准有多远只是一次减法，代码手里两个数都有。我却把一个算术事实交给她判断，还让它和别的选项分概率。改成代码里的阈值，问题就没了。

## 怎么确认改动有效

每改一次提示词，缓存全部失效。我前后跑了四轮全量评估，花了 6.8 美元，其中约 4.5 美元本可以省下。后来的做法是把改动攒在一起，先用 300 条样本验证（约 3 美分），并在运行之前把预期结果提交到 git，免得事后改口。这一轮四项指标全部按预期移动，总共花了 0.05 美元。

## 那 +107.8% 算不算数

不算。这个组合只有 5 个仓位，一年大约 22 次买入，碰巧买到哪几只对结果影响很大。我从股票池里随机删掉 49 只（正好是价格数据真实缺失的数量），重算信号，让 Jev 版和纯规则版在同一个池子上各跑一遍再比较，重复 12 次。结果是 Jev 平均多赚 4,048 美元，标准差 25,029 美元，12 次里赢 7 次，和抛硬币差不多。

同样的测量在这些修正之前是平均少赚 23,315 美元，12 次里只赢 1 次。所以能证明的是：补上证据、拿掉题干里的结论之后，Jev 从稳定地不如机械规则，变成了和规则分不出高下。这是进步，但还不是优势。下一步该动的是组合构造，而不是提示词。

## 总结

Jev 的能力不会超出这一次请求。把事实放进 state，把结论留在外面，给不确定性留出口，然后看置信度而不是答案。问一句"今天星期几"，就足以把这些都看清楚。
